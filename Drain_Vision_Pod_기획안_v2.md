# Drain Vision Pod — 천안시 빗물받이 감지·예측 시스템 기획안 (v2)

> v2 개정 요약: (1) 카메라는 **차량 탑재형**(고정 부착이지만 차량 자체는 이동)으로 아키텍처를 확정. (2) 부서 직접 협의 트랙은 제작 기한(1박 2일) 제약상 제외하고 전량 공개 자료 기반 설계로 명시. (3) 데모 지연·화질 이슈는 "예산 제약으로 고사양 가속기 대신 저비용 구성을 택한 결과"로 정면 설명.
>
> v2.1 개정 요약 (발표·스토리텔링 중심 심사에 맞춘 조정): (1) 예측 가중치 레이어를 정적 lookup에서 기상청 단기예보 API 연동 기반 **동적 예측**으로 고도화(2.2절, 9.2~9.4절) — 강수 예보 발생 시 우선순위 지도가 반응하는 시연 장면 확보(7장). (2) 우천 시 카메라 성능 저하 한계는 인정하되, 핵심 유즈케이스를 "폭우 중 실시간 대응"이 아닌 "**예보 발생 이전 사전 예방적 준설**"로 재프레이밍(8장). (3) DB를 PostgreSQL+Docker에서 **SQLite(파일 기반)**로 축소 — 데모 규모 대비 과설계를 줄이고, ORM 계층 유지로 확장 시 PostgreSQL 전환 경로를 발표 근거로 활용(9.1, 9.5, 9.6절).
>
> v2.2 개정 요약: (1) GPS 매칭을 **노선 기반 매칭**으로 고도화(2.1절) — 쓰레기차·버스처럼 노선이 고정된 차량은 배포 전 노선상 빗물받이 목록을 미리 등록해, 전체 6만여 개소가 아닌 노선당 후보군 내에서만 최근접 매칭을 수행함으로써 저가 GPS 오차로 인한 오매칭 위험을 완화. (2) 확장 전략을 신규 2.3절로 명문화 — **쓰레기차(1단계, 넓은 커버리지) → 버스(2단계, 고정 노선)** 순으로 차량군을 단계적으로 편입하는 정성적 로드맵으로 제시하며, 구체적 투입 대수·시점은 의도적으로 제시하지 않는다. 각 차량에는 동일 구성의 Pod 1대만 추가되어 아키텍처 변경 없이 선형 확장됨을 근거로 삼는다(9.3절 `vehicles` 테이블로 뒷받침).
>
> v2.3 개정 요약: (1) priority_score 계산식에서 **강수확률(w3)을 제거**하고 가중치를 재배분(2.2절) — 천안시 전역이 사실상 동일한 예보 격자권이라 drain별 차등 반영의 실효성이 낮았고, 대신 시스템의 핵심 신호인 **차폐율(occlusion, 신규 가중치 0.50)** 을 최우선으로 격상. 정적 위험도(고도 0.20 + 침수이력 0.30)와 실시간 감지(차폐율 0.50)가 대등하게 맞서되, 세 요소 중에서는 차폐율이 단독 최대 가중치를 갖도록 재설계. (2) 강수확률은 개별 drain 점수 대신 **시스템 전역 사전 알림**으로 용도를 전환 — 내일자 강수확률이 임계값(70%) 이상이면 대시보드 전체에 "내일 호우 예보 — 사전 점검 권장" 배너를 띄우는 방식으로, "폭우 발생 이전 사전 예방적 준설"이라는 8장의 기존 포지셔닝과 더 직접적으로 연결된다.
>
> v2.4 개정 요약 (9.3~9.5절 기술 스펙을 실제 구현과 동기화 + 신규 기능 4종): (1) **drain-노선 관계를 다대다로 전환**(9.3절) — `drains.route_id` 단일 컬럼 대신 `drain_routes` 조인 테이블 도입. 빗물받이 하나가 쓰레기차·버스 등 서로 다른 노선에 동시에 걸칠 수 있다는 2.3절 확장 전략의 현실을 스키마에 반영. (2) **감지 무응답 시 자동 OCCLUDED 추론**(2.1절과 연결) — Pod가 감지 결과와 무관하게 매 주기 보내는 위치 텔레메트리(`POST /api/telemetry`)를 근거로, 특정 drain 근접(5m) 신호 이후 일정 시간 진짜 판정이 도착하지 않으면 서버가 스스로 OCCLUDED로 기록. GPS는 왔는데 판정이 유실되는 통신 장애 상황에서도 이력 공백이 남지 않도록 하는 로드맵 보완. (3) **팀 수 기반 동선 추천**(`POST /api/route/plan`, 9.4절 신규) — 우선순위 상위 빗물받이를 팀 수만큼 좌표 기준 클러스터링한 뒤 팀별 방문 순서를 근사 최적화해 제시. TSP는 NP-hard라 완전탐색 대신 근사 알고리즘을 쓴다는 점을 응답에 항상 명시(정직성 원칙 유지). (4) **차량별 조회**(`GET /api/vehicles/{id}/drains`, 신규) — 특정 차량이 실제로 확인한 빗물받이 목록을 별도로 조회 가능하게 함. (5) 지대 고도 조회를 실제 공개 API(Open-Elevation) 연동으로 구현, API 키는 `.env` 파일로 분리.
>
> v2.5 개정 요약 (날씨 3단계 모드 — "게이트"와 "순서"를 분리해 두 가지 적응 메커니즘을 동시 적용, 2.2·2.2-1·2.2-2절): v2.3에서 강수확률을 drain별 점수에서 빼고 시스템 전역 배너로 분리했는데, "평소와 비 예보 시 판단 기준이 똑같이 고정이어도 되는가"라는 문제의식에서 한 걸음 더 나아갔다. **① 점검 임계값(게이트)**: 평소엔 차폐율 70% 이상만 점검 대상으로 잡고, 비 예보 시 50%, 폭우 예보 시 40%로 문턱을 낮춰 더 가벼운 막힘도 선제적으로 잡아낸다. **② 우선순위 가중치(순서)**: 평소엔 저지대·침수이력 가중치를 0으로 두고 차폐율만으로 순수 유지보수 관점의 순서를 매기지만, 비/폭우 예보가 뜨면 2.2절의 기존 3요소 공식(고도 0.20+침수이력 0.30+차폐율 0.50)으로 전환해 리스크 큰 지점을 앞순위로 당긴다. 두 메커니즘은 **같은 날씨 모드 판정 하나(NORMAL/RAIN/HEAVY_RAIN)를 공유**하며 서로 다른 역할(게이트 vs 순서)을 맡으므로 중복되지 않는다. 날씨 모드 판정 자체도 고도화했다 — 기존엔 강수확률(POP) 단일 값이었지만, POP는 "올 확률"만 알려줄 뿐 "얼마나 올지"는 알려주지 못해(POP 90%짜리 이슬비 vs POP 60%짜리 물폭탄을 구분 못 함) 예상 강수량(PCP)까지 함께 판정에 반영하도록 보강했다(PCP 파싱 실패 시에만 POP 단독 임계값으로 근사 폴백).
>
> v2.6 개정 요약 (전체 재점검 후 발견한 4가지 개선 — 2.2·9.3·9.4절): (1) **판정 신뢰도(confidence) 실사용** — 지금까지 수집만 되고 어디에도 안 쓰이던 값을, 엣지의 방문 집계(aggregate_visit)에서 단순평균 대신 confidence 가중평균으로 반영해 신뢰도 낮은 프레임의 영향력을 줄임. (2) **미점검 경과일(staleness)을 우선순위 4번째 요소로 신규 추가** — 고도·침수이력·차폐율에 이어 "마지막 점검 후 얼마나 지났는가"를 항상 0.15 가중치로 반영(날씨 모드와 무관하게 고정), 동시에 "며칠 지나야 시급한가"의 기준일은 날씨 모드별로 다르게(평소 14일, 비 예보 7일, 폭우 예보 1일 — "폭우 전엔 사실상 전수 점검"을 근사) 적용해 게이트에도 반영. 침수 이력 없고 차폐율도 낮지만 오래 방치된 지점이 계속 순위 밖에 머무는 문제를 해결. (3) **`/api/route/plan`의 게이트 파라미터를 단위별로 분리** — 기존 `min_priority` 하나가 상황에 따라 차폐율(%)과 가중 점수(0~1)를 섞어 의미하던 API 설계 결함을 `min_occlusion_pct`/`min_priority_score` 두 필드로 명확히 분리. (4) **"우선순위 근거" 패널에 현재 가중치 표시** — 가중치가 0인 요소(예: 평소 모드의 고도 위험도)도 값만 있으면 크게 표시되어 "왜 반영이 안 됐지"라는 오해를 유발하던 문제를, 각 지표 옆에 현재 가중치를 함께 표시하고 0-가중치 항목은 회색으로 처리해 해결.
>
> v2.7 개정 요약 (감지 무응답 판정을 "가짜 OCCLUDED"에서 "미점검(UNCHECKED)"으로 정정, 2.1·9.3절): v2.4에서 도입한 "근접 텔레메트리 후 일정 시간 진짜 판정이 안 오면 자동 OCCLUDED" 로직이, 사실은 "차폐 여부를 측정했다"가 아니라 "다른 차량 등에 시야가 가려져 아예 아무 신호도 못 받았다"는 별개의 사실을 차폐 100%라는 가짜 실측값으로 위장하고 있었다는 문제를 바로잡았다. 이제는 근접했는데 신호가 **하나도** 없었을 때만 이력에 `UNCHECKED`(미점검)로 남기고, `drains` 테이블의 현재 상태(last_status/last_occlusion_pct/last_updated)는 건드리지 않는다 — 그 결과 "마지막 진짜 점검"으로부터의 미점검 경과일(2.2-2절)이 이 실패한 시도 때문에 리셋되지 않고 정직하게 계속 쌓여서, 결국 미점검 게이트가 다시 그 지점을 점검 대상으로 끌어올린다. 반대로 그 근접 구간에서 신호가 하나라도(설령 그 값 자체가 이상값이라 나중에 걸러지더라도) 도착했다면, 이 로직은 절대 개입하지 않는다 — 판단은 전적으로 엣지의 방문 집계(이상값 제거+평균)에 맡긴다.
>
> v2.8 개정 요약 (실도로망 동선 추천 + 사람의 조치/점검 완료 워크플로우, 2.2-3·9.3·9.4절): (1) **동선 추천에 OSRM 실도로망 거리 연동** — 지금까지 직선거리(haversine)로만 계산하던 팀별 방문 거리를, OSRM 공개 데모 서버의 Table API로 실제 도로망 기준 거리로 대체했다. 지리적 팀 분류(클러스터링) 단계는 여전히 직선거리를 쓰지만(어느 대략적 구역인가는 정밀도가 덜 중요), 각 팀 안에서의 실제 방문 순서·거리는 이제 도로를 따라가는 값이다. OSRM 공개 서버는 요청량 제한이 있는 비상용 서비스라, 실패/타임아웃 시 조용히 직선거리로 폴백하며 응답에 `distance_source`(osrm/haversine)를 그대로 노출해 어느 쪽이 쓰였는지 숨기지 않는다. (2) **사람이 처리한 결과를 "조치 완료/점검 완료"로 기록**(신규 `POST /api/drains/{id}/resolve`, 2.2-3절 신규) — 정비원이 실제로 청소했거나, 미점검 상태였던 지점을 사람이 직접 확인했을 때 버튼 하나로 표시할 수 있다. 다만 AI의 실측값(last_status/last_occlusion_pct)은 위조하지 않고 그대로 둔 채 "사람이 언제 처리했는지"만 별도로 기록하며, 그 시각을 미점검 경과일(2.2-2절 staleness) 계산의 새 기준점으로 삼는다 — 그 결과 "조치 완료했고 이후엔 차량 순찰로 재확인 예정"이라는 문구 그대로, 처리 직후엔 점검 목록에서 빠졌다가 날씨 모드별 기준 일수가 다시 지나면 차량 재확인 대상으로 자연스럽게 돌아온다. 별도의 일정 관리 로직 없이 기존 staleness 메커니즘을 그대로 재사용한 설계다.
>
> v2.9 개정 요약 (데모용 날씨 모드 수동 전환, 2.2-2절): 2.2-2절의 3단계 날씨 모드는 실제 기상청 예보에 의존하는데, 발표 당일 실제 날씨가 세 모드를 다 보여줄 가능성은 낮다. 이를 보완하기 위해 `POST /api/weather/mode`를 신규 추가해, 발표자가 대시보드 버튼(평소/비 예보/폭우 예보) 하나로 시스템 전역 모드를 즉시 강제 전환하고 게이트·가중치·배너가 실제로 바뀌는 걸 시연할 수 있도록 했다. 수동 전환 중에는 `POST /api/priority/refresh`(실제 예보 갱신)가 호출돼도 강제한 모드가 되돌아가지 않으며, "자동(실제 예보)" 버튼으로만 해제된다 — 이는 어디까지나 실제 예보 연동(2.2-2절 본 메커니즘)을 대체하는 게 아니라, 그 메커니즘이 만드는 효과를 데모 시점에 재현하기 위한 발표용 스위치임을 명확히 한다.
>
> v2.10 개정 요약 (수동 전환 UI 정리 + 폭우 판정 기준을 실제 기상청 수치로 교정, 2.2-2절): (1) v2.9의 3버튼(평소/비 예보/폭우 예보) UI가 "자동" 버튼과 뒤섞여 다소 산만했던 것을, "수동" 버튼 하나 + 그 아래 드롭다운(평소/비 예보/폭우 예보 중 선택) 조합으로 정리했다 — "자동(실제 예보)"는 별도 버튼으로 남아 언제든 실제 예보로 복귀 가능하다. (2) **폭우 판정 임계값을 실제 기상청 수치로 교정** — 기존 "1시간 강수량 30mm 이상 → 폭우"는 검증된 공식 기준이 아닌 자체 추정치였다. 검색으로 확인한 기상청의 실제 폭우 관련 특보 기준은 호우주의보(3시간 60mm 또는 12시간 110mm 이상)·호우경보(3시간 90mm 또는 12시간 180mm 이상)가 기본이며, 2023.6.15부터는 이보다 강한 "극한호우" 특보가 시간당 50mm 이상(+3시간 90mm 이상) 또는 시간당 72mm 이상 단독 기준으로 별도 운영되고 있다. 우리 시스템은 기상청 단기예보 API의 PCP가 1시간 단위 값만 제공해 3시간 누적 조건은 계산할 수 없으므로, 그중 단기예보 데이터로 바로 검증 가능한 "시간당 50mm 이상"(극한호우 기준의 단일 조건 쪽)을 채택해 `HEAVY_RAIN_PCP_MM`을 30→50으로 상향했다. 이 근사가 갖는 한계(3시간 누적 조건 미반영)는 로드맵 항목으로 명시한다.
>
> v2.11 개정 요약 (판정 상태·점검 목록·동선 UI 정교화): (1) 모델이 빗물받이 마스크를 전혀 찾지 못한 경우를 실제 차폐가 측정된 `OCCLUDED`와 분리해 `UNDETECTED`(미검출, 차폐율 없음)로 기록한다. 방문 프레임이 전부 미검출일 때만 이 상태를 확정하며, 텔레메트리만 있고 최종 판정 자체가 없었던 `UNCHECKED`(미점검, 이력 전용)와도 구분한다. (2) 대시보드 우선순위 목록은 전체 빗물받이가 아니라 날씨별 차폐율·미점검 게이트 또는 미검출 조건을 만족한 실제 점검 대상만 표시한다. (3) 차량별 조회를 선택하면 그 차량이 판정한 빗물받이만 지도에 표시하고 화면 범위를 자동 조정한다. (4) 동선은 위치 기반 군집을 먼저 만들고, 팀별 최고 우선순위 지점을 첫 방문지로 고정한 뒤 최근접 이웃+2-opt로 남은 순서를 개선해 같은 지역에서의 불필요한 왕복을 줄인다.
>
> v2.12 개정 요약 (추론·검출·판정 용어 및 운영 오류 분리): 엣지는 모든 프레임에서 추론을 실행하지만 빗물받이 객체가 항상 검출되는 것은 아니다. 따라서 기존 `UNDETECTED`를 물리 상태처럼 보이지 않는 `UNASSESSABLE`(판정 불가)로 바꾸고 사유 코드 `DRAIN_NOT_DETECTED`를 함께 기록한다. 텔레메트리 이후 판정이 도착하지 않은 경우도 `UNCHECKED`라는 빗물받이 판정에서 제거하고, 별도 `system_events` 테이블의 `RESULT_MISSING` 장치·통신 이벤트로 분리한다. 대시보드 상세 화면 역시 판정 이력과 시스템 이벤트를 별도 영역으로 표시한다.
>
> v2.12 개정 요약 (폭우 판정을 "극한호우"에서 "호우주의보" 수준으로 재조정 + 진짜 3시간 누적 계산, 2.2-2절): v2.10에서 "극한호우"(시간당 50mm) 기준을 잠깐 채택했으나, 이는 기상청이 "호우"(=폭우) 용어를 쓰기 시작하는 지점인 호우주의보보다 훨씬 위의 비상 등급이라 실전에서 거의 발동하지 않는 문제가 있었다. 이를 "폭우 예보 모드가 KMA의 호우주의보 수준과 사실상 같아야 한다"는 방향으로 재조정했다 — 호우주의보의 실제 발표 기준(3시간 누적 강수량 60mm 이상)을 그대로 채택했다. 다만 기상청 단기예보 API는 1시간 단위 PCP만 제공하므로, 이번엔 그 1시간 값 하나를 억지로 3시간 기준에 대입하는 근사 대신 **실제로 연속된 시간대 3개를 롤링 합산**해(`forecast._max_rolling_3h_pcp`) 진짜 3시간 누적치를 구성한 뒤 60mm와 비교하도록 구현을 정교화했다 — 하루 중 어느 3시간 구간이든 기준을 넘으면 그 최댓값으로 판정한다. 연속된 시간대 데이터가 부족하면(드문 경우) 기존처럼 강수확률(POP) 단독 임계값으로 안전하게 폴백한다. `HEAVY_RAIN_PCP_MM`(1시간 기준)은 `HEAVY_RAIN_PCP_3H_MM`(3시간 누적 기준, 60.0)으로 대체됐다.
>
> v2.13 개정 요약 (동선 추천에서 우선순위 고정 시작점 제거 + or-opt 추가, 9.4절): v2.11에서 도입한 "팀별 최고 우선순위 지점을 첫 방문지로 고정" 방식을 실사용해보니, 그 지점이 팀 담당 구역 안에서 지리적으로 동떨어져 있을 때 전체 동선이 그쪽으로 갔다가 되짚어오는 왕복을 만들었다. 2-opt(구간 뒤집기)만으로는 이 왕복을 못 없앤다는 걸 코드로 직접 검증했다 — 반환된 경로가 2-opt 기준으로는 이미 완전히 수렴된 상태(개선 가능한 swap 0개)인데도 수 km짜리 불필요한 왕복이 남아 있었다. 원인은 "이미 지나온 구역에 남겨둔 지점 몇 개를 다른 위치로 옮겨 끼우는" 유형의 개선이 2-opt의 이웃 구조(반전)에는 아예 없다는 것이었다. 그래서 (1) or-opt(길이 1~3 구간을 통째로 뽑아 재삽입)를 추가해 2-opt와 번갈아 수렴할 때까지 지역 탐색하고, (2) "최고 우선순위 지점을 억지로 첫 방문지로 고정"하는 규칙 자체를 없앴다 — 어떤 지점이 점검 대상에 들어갈지(게이트)와 얼마나 시급한지(우선순위 점수·목록 정렬)는 이미 다른 곳에서 처리되고, 같은 팀은 배정된 지점을 어차피 전부 방문해야 하므로 방문 "순서"는 순수하게 총 이동거리를 최소화하는 편이 현장에 더 도움이 된다는 판단이다.
>
> v2.14 개정 요약 (BLOCKED/OCCLUDED 차폐율 경계값 역전 수정, 2.1·3·9.3절): `edge/infer.py::classify()`가 차폐율 15~60%를 `BLOCKED`(막힘), 60% 이상을 `OCCLUDED`(부분 차폐)로 분류하고 있었다 — "막힘"이 "부분 차폐"보다 심각한 상태라는 국문 어감, 그리고 대시보드가 이미 BLOCKED를 더 위험한 색(빨강)·지도 범례 상위로 취급하던 기존 관례와 정확히 반대였다. 차폐율이 낮을수록(부분적으로만 가려짐) `OCCLUDED`, 높을수록(사실상 완전히 막힘) `BLOCKED`로 배정하도록 경계값을 맞바꿨다(`infer.py`의 `BLOCKED_MAX_PCT` 상수를 `OCCLUDED_MAX_PCT`로 개명). 같은 이유로 엣지 방문 집계(`run.py::aggregate_visit`)의 동률 심각도 우선순위도 `BLOCKED > OCCLUDED > CLEAR`로 맞바꿨다. 데모 시드 데이터(`db/drains_seed.json`)의 기존 판정 92건 중 두 상태가 뒤바뀌어 있던 36건도 함께 정정했다.

## 1. 프로젝트 개요

### 1.1 문제 정의
천안시 빗물받이 6만 1,116개소는 현재 사람이 직접 점검·준설하는 방식으로 관리된다. 천안시가 추진 중인 107억 원 규모 온디바이스 AI 침수대응 사업(하천·지하차도 대상 스마트폴 + 공원 산책로 순찰용 사족보행 로봇)과 환경부의 국가 도시침수예보체계(유역 단위 강우·수위 예측) 모두 하천·지하차도 등 거시적 스케일을 다루며, 개별 빗물받이 grate 단위의 실시간 상태 감지는 다루지 않는다. 본 프로젝트는 이 공백을 메우는 "눈(sensor layer)"을 기존 체계에 추가하는 보완 제안으로 포지셔닝한다.

### 1.2 포지셔닝 원칙 — 차량 탑재형 확정에 따른 재정리
- 국가·천안시 사업에 대응하거나 경쟁하는 것이 아니라 플러그인 형태의 보완 제안.
- **핵심 차별화 지점 재확인**: LG유플러스·에이아이매틱스의 차량탑재 AI 시설물 감지 서비스는 포트홀·막힌 빗물받이 등을 하나의 클래스로 묶어 "이상 여부"만 판정하는 범용 도로시설물 탐지 서비스다. 본 프로젝트는 동일하게 **차량 탑재형** 배치를 취하지만, (1) 빗물받이 단일 대상에 특화된 세그멘테이션으로 **차폐율(%)이라는 정량 지표**를 산출하고, (2) 그 결과를 지대 높이·침수 이력과 결합해 **준설 우선순위를 자동 스케줄링하는 예측 레이어**까지 연결한다는 점에서 "탐지"에 그치는 경쟁 서비스와 구조적으로 다르다. 두 서비스 모두 차량 탑재형이라는 사실 자체는 차별화 포인트가 아니므로, 발표에서는 이 점을 먼저 인정하고 "판정의 정밀도(정량적 %)"와 "판정 이후의 활용(우선순위 산출)" 두 가지로 차별화를 좁혀서 설명한다.
- 실제 시스템 연동은 "확정"이 아닌 "제안·로드맵" 수준으로 발표에 명시한다. 다문화가족지원센터 프로젝트와 마찬가지로, **천안시 관련 부서와의 직접 협의는 진행하지 않았으며 전량 공개 자료(공공데이터포털, UPIS, 언론 보도, 환경부 지정 침수 중점관리구역 자료)에 기반한 설계**임을 명시한다. 제작 기한이 1박 2일로 매우 제한적이라 사전 문의 절차 자체를 계획에서 제외했다.

## 2. 시스템 구조 (2계층, 차량 탑재형 기준)

### 2.1 감지 레이어 (코어)
- 카메라 + 파인튜닝된 YOLOv8n-seg 모델이 라즈베리파이(엣지) 상에서 온디바이스로 실시간 판정.
- **차량 탑재 방식**: 카메라는 순찰·준설 차량(또는 시연 시 이를 모사한 이동 지그)에 고정 브래킷으로 부착. 카메라 자체는 고정되어 있으나 차량이 이동하며 여러 빗물받이를 순차적으로 지나가는 구조 — 즉 "고정 설치형 다지점 카메라"가 아니라 "이동형 단일 카메라가 다수 지점을 순회 촬영"하는 모델이다. 이는 6만여 개소 전수를 커버하기 위한 현실적인 확장 경로이기도 하다(지점당 카메라 1대를 상시 설치하는 것은 예산상 불가능하므로, 이미 도시 전역을 정기 순회하는 기존 차량군에 Pod를 편입시켜 저비용으로 커버리지를 확보하는 방향 — 구체적 확장 방식은 2.3절 참조).
- 판정 상태: CLEAR / OCCLUDED / BLOCKED / UNASSESSABLE 4종(v2.14, 심각도 낮은 순). `OCCLUDED`(부분 차폐)는 마스크가 검출되어 계산된 차폐율이 15% 이상 60% 미만인 경우, `BLOCKED`(막힘)는 60% 이상인 경우다 — "막힘"이 "부분 차폐"보다 심각한 상태라는 국문 어감에 맞춰 차폐율이 더 큰 쪽에 배정했다. `UNASSESSABLE`은 추론은 실행됐지만 빗물받이 객체를 찾지 못해 물리 상태를 판정할 수 없는 경우이며, `reason_code=DRAIN_NOT_DETECTED`, 차폐율 `null`로 기록한다.
- "대상 미검출 ≠ 완전 차폐 확정" 원칙: 카메라 각도·조명·모델 실패와 실제 완전 가림을 현재 구성만으로 구분할 수 없으므로 `OCCLUDED`로 추정하지 않는다. 판정 결과 자체가 서버에 도착하지 않은 경우는 빗물받이 상태가 아니라 `RESULT_MISSING` 시스템 이벤트다.
- 판정 결과(상태 + 신뢰도 + 차폐율% + 위치정보)만 서버로 전송하며, 영상 원본은 전송하지 않음 — 통신 안정성 확보 및 지연 최소화.
- **위치 태깅 및 매칭 방식(v2.2 개정 — 노선 기반 매칭)**: 차량이 이동하며 다수 지점을 지나가므로, 각 판정이 "어느 빗물받이"에 대한 것인지 식별하는 위치 정보가 필수적으로 추가된다. GPS 모듈(또는 차량 자체 텔레매틱스 GPS 활용)로 위·경도를 획득한다.
  - 단순 전역(全域) 최근접 매칭은 저가 GPS의 수 미터 오차로 인해 도심 밀집 구간에서 인접한 두 빗물받이를 혼동할 위험이 크다. 이를 완화하기 위해 2.3절 확장 전략(쓰레기차·버스 등 고정 노선 차량의 순차 편입)의 특성을 그대로 활용한다: 각 차량은 담당 노선(쓰레기 수거 구역, 버스 노선)이 고정되어 있으므로, 배포 전 해당 차량이 실제로 지나가는 빗물받이 목록만 천안시 공공데이터포털 좌표를 기준으로 미리 등록(route 등록)해 둔다.
  - **매칭 절차**: (1) GPS 좌표 수신 → (2) 해당 차량에 등록된 노선상의 빗물받이 후보군으로 검색 범위를 좁힘(전체 6만여 개소가 아니라 노선당 수십~수백 개소) → (3) 좁혀진 후보군 내에서만 최근접 매칭(nearest-neighbor matching) 수행. 후보군이 좁아질수록 동일한 GPS 오차 범위 내에서 오매칭될 확률이 크게 낮아진다.
  - 노선이 아직 등록되지 않은 신규 차량·구간에 대해서는 기존의 전역 최근접 매칭을 fallback으로 유지한다.
  - 그럼에도 동일 노선 내에서 두 빗물받이가 매우 가깝게 위치한 경우(예: 교차로 양쪽)의 잔여 혼동 가능성은 남아 있으며, 이는 로드맵 항목(RTK-GPS, 진행 방향·속도 기반 필터링, 통과 순서 기반 보정)으로 유지한다.

### 2.2 예측 가중치 레이어 (보조) — v2.6: 미점검 경과일을 4번째 요소로 추가
- 목적: 차량이 순회할 때 어느 구간·어느 빗물받이를 **더 자주** 지나가도록(또는 우선 순찰 경로에 포함하도록) 동선을 조정할지 결정하는 스케줄링 로직. (v1의 "카메라를 어디에 배정할지"에서 "차량 동선을 어떻게 우선순위화할지"로 재정의)
- **입력 변수 구성 (정적 위험도 + 실시간 감지 + 점검 시급성)**:
  - 정적 변수: 지대 높이(국토지리정보원 DEM) + 과거 침수이력(환경부 지정 침수 중점관리구역: 성정동·신부동·원성동·구성동) — 지역별 기저 위험도(baseline risk)를 형성.
  - 실시간 변수: 감지 레이어(2.1절)가 산출한 최신 차폐율(last_occlusion_pct).
  - **점검 시급성 변수(v2.6 신규)**: 마지막 점검(last_updated) 후 경과일 — 침수 이력도 없고 지금은 안 막혀 있어도, 오랫동안 아무도 확인하지 않은 지점은 그 자체로 리스크다. "얼마나 지났는지"를 점수로 환산.
  - 계산식(단순 가중합 — 1박2일 내 구현 가능한 수준으로 의도적으로 단순화): `priority_score = w1×elevation_risk + w2×flood_history_flag + w3×staleness_risk + w4×occlusion_norm`.
- **가중치(w1~w4)는 2.2-2절의 날씨 모드에 따라 전환된다**:
  - **평소(NORMAL)**: `w1=0, w2=0, w3=0.15, w4=0.85` — 저지대·침수이력은 반영하지 않고 **차폐율(0.85)+미점검 경과(0.15)**로만 순위를 매긴다. 비가 오지 않는 평상시엔 저지대·침수이력이라는 "재해 리스크" 요인이 당장의 점검 우선순위와 무관하다고 보고, 실시간 영상 인식과 점검 주기 관리라는 순수 유지보수 관점으로 단순화했다.
  - **비 예보(RAIN) / 폭우 예보(HEAVY_RAIN)**: `w1=0.15, w2=0.25, w3=0.15, w4=0.45` — 비가 임박하면 저지대·침수이력이라는 재해 리스크가 실질적 의미를 가지므로 다시 반영하되, 차폐율이 여전히 단독 최대 가중치를 유지한다.
  - **미점검 경과 가중치(w3=0.15)는 날씨 모드와 무관하게 항상 고정**이다 — "얼마나 지났으면 시급한가"의 판단은 날씨에 따라 달라지지만(아래 게이트 참조), "지났다는 사실 자체가 리스크"라는 원칙은 날씨와 무관하기 때문이다.
  - 가중치는 데모 단계에서 고정값으로 하드코딩하고, 로드맵에서 "실제 침수 데이터 축적 후 회귀분석 기반 보정"으로의 발전 방향을 명시한다.
- 제외 항목: 배관 통수량 — 천안시 공공데이터포털·UPIS 데이터 확인 결과 관경/통수량 등 수리학적 데이터는 개방되어 있지 않음(도형관리번호·라벨명·면적·길이만 제공). 통수량 및 준설·정비 이력은 로드맵 항목으로 유지하며, 부서 직접 협의는 이번 제작 범위에서 진행하지 않았음을 명시한다.

### 2.2-1 강수 예보 — 시스템 전역 사전 알림 (v2.3 신규, v2.5 확장)
- 배경: 강수확률을 drain별 priority_score에 넣던 기존 방식(v2.1~v2.2)은 "천안시 전역이 거의 같은 확률값을 공유하는데 굳이 지점마다 반복 조회·반영할 이유가 있는가"라는 문제를 안고 있었다. v2.3부터는 이를 **drain 단위 차등 반영이 아니라 도시 전체 단위의 사전 알림**으로 재설계했다.
- 동작 방식: 백엔드가 천안시 대표 좌표(시청) 1곳에 대해서만 기상청 단기예보 API를 조회해 **내일자** 강수확률·강수량을 얻고, 2.2-2절의 날씨 모드로 분류한다. 가장 심각한 **폭우 예보(HEAVY_RAIN)** 모드일 때만 "내일 천안시 전역 호우 예보 — 전체 구간 사전 점검 권장" 알림을 대시보드 전체에 큰 배너로 띄운다. 특정 drain의 순위를 끌어올리는 대신, 순찰팀 전체가 비가 오기 전날 미리 대응하도록 유도하는 것이 목적이다.
- 이 설계는 8장에서 이미 밝힌 "폭우 중 실시간 대응"이 아니라 "**강수 예보 발생 이전의 사전 예방적 준설**"이라는 핵심 유즈케이스와 더 직접적으로 맞물린다 — 개별 지점 우선순위가 아니라 전체 순찰 일정을 앞당기는 신호로 쓰이기 때문이다.
- 로드맵: 데모 단계의 임계값들은 의도적 단순화이며, 실제 운영 시에는 호우주의보/경보 발표 연동 등으로 정교화가 필요하다.

### 2.2-2 날씨 3단계 모드 — 게이트(점검 임계값)와 순서(가중치)의 동시 적용 (v2.5 신규, v2.6 확장)
- 문제의식: v2.3~v2.4는 "비 예보가 뜨면 배너 하나만 띄운다"는 수준이었다. 그런데 실제로는 평소와 비 오기 전의 **점검 판단 기준 자체**가 달라야 한다 — (1) 얼마나 막혀야/얼마나 오래됐어야 점검할지의 **문턱**, (2) 어떤 지점을 먼저 갈지의 **순서**, 이 두 가지 모두.
- 이 두 메커니즘은 서로 다른 질문에 답하므로 역할을 분리해 **동시에** 적용한다:
  - **게이트(점검 임계값, 우선순위 목록과 `POST /api/route/plan` 기본 후보의 공통 기준)**: "오늘 점검 대상에 넣을지"를 세 가지 독립 조건 중 하나라도 만족하면 넣는다 — ① 차폐율이 모드별 기준 이상, ② 마지막 점검 후 경과일이 모드별 기준 이상, ③ 최신 판정이 `UNASSESSABLE`. `RESULT_MISSING`은 운영 오류이므로 물리 상태 게이트를 직접 변경하지 않는다.
  - **순서(우선순위 가중치, 2.2절)**: "넣었다면 누구부터 갈지"를 결정. 평소엔 차폐율+미점검 경과만, 비/폭우 예보 시엔 저지대·침수이력까지 반영.
- **날씨 모드 판정**: 기존엔 강수확률(POP) 하나만 봤지만, POP는 "올 확률"만 알려줄 뿐 "얼마나 올지"는 알려주지 못한다(POP 90%짜리 이슬비와 POP 60%짜리 물폭탄을 구분 못 함). v2.5부터 예상 강수량(PCP)도 함께 조회하는데, v2.12부터는 매 시간대 PCP 값을 다 모아 **실제로 연속된 3개 시간대를 롤링 합산**해 하루 중 최대 3시간 누적 강수량을 구성하고(`forecast._max_rolling_3h_pcp`), 이 값을 우선 신뢰한다(60mm 이상 → 폭우). 연속된 시간대가 3개 미만이라 계산할 수 없을 때만 POP 단독 임계값(40%/70%)으로 근사한다. **60mm는 임의 추정치가 아니라 기상청 호우주의보의 실제 발표 기준(3시간 누적 강수량 60mm 이상, 또는 12시간 누적 110mm 이상)에서 3시간 조건을 그대로 가져온 값이다**(v2.12) — 호우주의보는 기상청이 "호우"(=폭우) 용어를 처음 쓰기 시작하는 지점이라 이 시스템의 "폭우 예보" 라벨과 의미가 가장 가깝다. v2.10에서는 이보다 훨씬 드문 "극한호우"(2023.6.15 신설, 시간당 50mm 이상+3시간 90mm 이상 또는 시간당 72mm 단독) 기준을 시간당 근사로 잠깐 채택했었지만, 실전에서 거의 발동하지 않는 문제가 있어 되돌렸다. 참고로 호우경보(3시간 90mm·12시간 180mm 이상)는 그보다 한 단계 더 심한 등급인데, 이 시스템은 3단계(평소/비/폭우)만 두므로 별도 등급으로 구분하지 않는다.

| 모드 | 판정 기준 (근사) | 점검 게이트(차폐율 OR 미점검일) | 우선순위 가중치(고도/침수이력/미점검/차폐율) |
|---|---|---|---|
| 평소(NORMAL) | 3시간 누적 PCP<60mm 이고 POP<40% (또는 PCP 확인상 0) | 70% 이상 OR 14일 이상 | 0 / 0 / 0.15 / 0.85 |
| 비 예보(RAIN) | 3시간 누적 PCP 0~60mm 이거나 POP≥40% | 50% 이상 OR 7일 이상 | 0.15 / 0.25 / 0.15 / 0.45 |
| 폭우 예보(HEAVY_RAIN) | 3시간 누적 PCP≥60mm (기상청 호우주의보 기준, 또는 폴백 시 POP≥70%) | 40% 이상 OR 1일 이상 | 0.15 / 0.25 / 0.15 / 0.45 |

- 대시보드에는 현재 모드와 적용 중인 점검 기준을 항상 작은 표시로 띄우고(예: "현재 모드: 비 예보 · 점검 기준 차폐율 50% 이상 · 미점검 7일 이상"), 폭우 예보일 때만 2.2-1절의 큰 경고 배너를 추가로 띄운다 — 평소엔 조용히, 위험할 때만 크게 알리는 방식으로 배너 피로도를 관리한다. "우선순위 근거" 패널에도 현재 모드의 가중치를 각 지표 옆에 함께 표시하고, 가중치가 0인 지표는 회색으로 처리해 "값은 있는데 왜 반영이 안 됐지"라는 오해를 막는다(v2.6).
- 로드맵: 지금의 임계값 중 POP 40/70%와 게이트 일수(14/7/1일)는 여전히 데모용 근사치다. 3시간 누적 PCP 60mm는 기상청 호우주의보 기준을 그대로 가져왔지만(v2.12), 그보다 위 등급인 호우경보(90mm)·극한호우는 별도로 구분하지 않는 3단계 단순화다. 실제 도입 시에는 침수 데이터 축적 후 보정, 그리고 필요하다면 4단계 이상으로의 세분화가 필요하다는 점을 2.2절과 동일한 톤으로 명시한다.
- **발표용 수동 전환**(신규 v2.9, UI 개편 v2.10): 실제 기상청 예보가 발표 당일 세 모드를 골고루 보여줄 가능성은 낮으므로, 대시보드에 "수동" 버튼(누르면 평소/비 예보/폭우 예보 드롭다운이 나타남)과 "자동(실제 예보)" 복귀 버튼을 두어 `POST /api/weather/mode`로 위 표의 게이트·가중치·배너를 즉시 재현할 수 있게 했다(9.4절). 수동 전환 중에는 `POST /api/priority/refresh`가 실제 예보를 재조회해도 강제한 모드를 덮어쓰지 않으며, "자동" 버튼을 눌러야만 실제 예보 기반 판정으로 복귀한다 — 실제 예보 연동 메커니즘 자체를 대체하는 게 아니라 데모 시점에 그 효과를 보여주기 위한 스위치다.

### 2.2-3 사람의 조치/점검 완료 워크플로우 (신규, v2.8)
- 배경: 지금까지는 "AI가 감지한다"만 있었지, "사람이 실제로 처리했다"는 별도 상태가 없었다. 정비원이 실제로 가서 청소했어도, 다음 차량이 그 지점을 다시 지나가기 전까지는 계속 우선순위 최상단에 남아 순찰팀을 헷갈리게 만드는 문제가 있었다.
- 동작 방식: 대시보드에서 특정 빗물받이에 "조치 완료"(BLOCKED/OCCLUDED) 또는 "점검 완료"(한 번도 점검 안 됐거나 UNASSESSABLE)를 누르면 그 시각이 기록된다. **AI의 실측값은 위조하지 않는다.**
- 이 설계의 핵심: 사람이 처리한 직후엔 그 지점이 점검 목록(동선 추천 후보)에서 자동으로 빠지지만, 별도의 일정 관리 로직을 새로 만들지 않는다 — 그냥 기존 미점검 경과일 게이트가 이 새 기준 시각부터 다시 카운트를 시작해서, 날씨 모드별 기준 일수(평소 14일/비 예보 7일/폭우 예보 1일)가 지나면 자연스럽게 다시 차량 재확인 대상으로 떠오른다. "조치 완료했고 이후엔 차량 순찰로 점검 예정"이라는 문구가 실제로 구현된 방식이 이것이다.
- 로드맵: 지금은 "조치 완료" 여부만 이진적으로 기록하지만, 실제 운영 시에는 담당자·조치 내용·사진 첨부 등 더 상세한 작업지시(work order) 이력 관리로 확장할 수 있다.

### 2.3 확장 전략 — 차량군 단계적 확대 (신규, v2.2)
- 목적: 6만여 개소 전수 커버를 위해 특정 차종에 카메라를 신규로 배치하는 대신, 이미 도시 전역을 정기적으로 순회하는 기존 차량군에 Pod를 순차적으로 편입시켜 자연스럽게 커버리지를 확장한다.
- **1단계 — 쓰레기차(청소차)**: 이면도로·골목까지 폭넓게 진입하는 순회 특성상 커버리지 범위가 가장 넓어 초기 검증 차종으로 적합.
- **2단계 — 버스**: 노선·운행 스케줄이 고정되어 있어(정류장·경로가 공개 정보로 확인 가능) 배포 전 노선상의 빗물받이 목록을 미리 등록할 수 있고, 이는 2.1절에서 정리한 노선 기반 GPS 매칭과 직접 맞물린다. 노선 단위로 Pod를 추가해가며 도시 도로망을 순차적으로 편입.
- **이후 단계**: 검증된 차종을 기반으로 순찰차 등 정기 운행 차량으로 추가 확장 가능. 이 단계의 구체적 투입 대수·시점은 앞선 단계에서 확보한 실측 데이터(차종별 실제 커버리지, 매칭 정확도)를 근거로 결정할 사안이므로, 검증되지 않은 숫자를 지금 단정적으로 제시하지 않고 로드맵으로 유보한다.
- **확장 단위의 단순성(발표 근거)**: 차종·노선이 달라져도 각 차량에는 동일 구성의 Pod(카메라+라즈베리파이+GPS 모듈, 4장 BOM) **1대만** 추가되며, 하드웨어·소프트웨어 구성 변경이 없다. 서버 측도 `vehicles` 테이블에 신규 차량 1행과 담당 노선만 등록하면 되므로(9.3절), 차량이 늘어나도 아키텍처 변경 없이 선형적으로 확장된다 — 대수 목표를 제시하지 않아도 확장 경로 자체가 구조적으로 실현 가능함을 근거로 제시한다.

## 3. 차폐율(%) 계산 방식

파인튜닝 제작 기한이 1주일 이상 확보되어, 단순 3클래스 분류가 아닌 세그멘테이션(YOLOv8n-seg) 기반으로 구체적 차폐율을 산출한다.

### 3.1 데모용 baseline 방식 (그대로 유지)
- 시연 Pod는 고정 지그가 정해진 경로로 3개 Drain(A/B/C)을 지나는 구조이므로, 각 Drain 위치·각도에서 완전히 깨끗한 상태의 baseline 사진을 1장씩 확보해 기준 픽셀 수로 사용.
- `차폐율(%) = (1 - 현재 마스크 픽셀 수 / 해당 Drain의 baseline 픽셀 수) × 100`
- 마스크가 전혀 잡히지 않으면 차폐율 100%(완전 가림)로 처리.
- 라벨링 시 "보이는 빗물받이 영역"만 폴리곤 1개로 그리며, 이물질 자체의 윤곽선은 별도로 그리지 않음. 폴리곤에는 CLEAR/BLOCKED/OCCLUDED 클래스를 동시에 태깅해 "왜" 가려졌는지와 "얼마나" 가려졌는지를 함께 확보.

### 3.2 실제 차량 탑재 배치 시의 한계 (정직하게 명시)
데모의 고정 지그는 거리·각도가 항상 동일하지만, 실제 차량 탑재 환경에서는 다음 이유로 baseline 방식이 그대로 적용되지 않는다.
- 도로 폭, 정차 위치, 차량 속도에 따라 카메라-grate 간 거리·각도가 매번 달라짐.
- 이 경우 baseline 픽셀 수 대비 상대 비교가 왜곡되어 차폐율이 부정확해질 수 있음.

**MVP 단계의 입장**: 본 프로젝트는 상용 수준의 정확도를 목표로 하지 않으며, 경진대회 시연 가능한 개념 증명(proof-of-concept) 수준의 MVP임을 전제로 한다. 따라서 이 한계는 숨기지 않고 **로드맵 항목**으로 명시한다.
- Phase 2 개선안: (1) 차량 브래킷 고정 각도·높이를 표준화해 도로면 대비 카메라 자세를 일정하게 유지 — 이는 완전한 해결책은 아니지만 편차 범위를 줄이는 실질적 조치임. (2) grate 표준 규격(KS 규격 등) 기반 픽셀-실면적 환산으로 baseline 없는 절대 차폐율 산출 방식으로 전환. (3) 저비용 ToF 센서 추가로 실측 거리 보정.
- 발표 스크립트 권장 문구: "현재 MVP는 고정 거리·각도를 가정한 baseline 비교 방식이며, 실제 차량 탑재 환경에서는 거리 편차로 인한 오차가 존재함을 인지하고 있습니다. 이는 예산과 제작 기간의 제약 속에서 개념 증명을 우선한 결과이며, Phase 2에서는 무보정 절대 차폐율 산출 방식으로 고도화할 계획입니다."

## 4. 하드웨어 구성 (BOM) — 차량 탑재형 반영

| 모듈 | 필수 여부 | 비고 |
|---|---|---|
| Raspberry Pi 5 (4GB) | 필수 | 엣지 연산 코어 |
| microSD 64GB (A2급) | 필수 | OS/모델/로그 저장 |
| 소형 광각 카메라 모듈 | 필수 | Pi Camera Module 3 Wide 또는 USB 광각캠 |
| **차량 거치 브래킷(마그네틱/흡착식)** | **필수(신규)** | 차량 탑재형 확정에 따라 추가. 대시보드 또는 사이드미러 부근 고정, 각도 조절 가능한 형태 권장 |
| **GPS 모듈(USB 또는 UART)** | **필수(신규)** | 위치 태깅용. 저가 NEO-6M류로 충분 — 노선 기반 매칭(2.1절)으로 오차로 인한 오매칭 위험을 완화하며, 잔여 오차는 로드맵 한계로 명시 |
| USB-C 보조배터리 (또는 차량 시거잭 12V→5V 변환) | 필수 | 데모는 보조배터리로 진행, 실차 배치 시 차량 전원 활용을 로드맵에 기재 |
| 액티브 쿨러(방열) | 추가 권장 | 세그멘테이션 모델 지속 추론 시 발열 방지 — CPU 단독 추론 시 열 스로틀링으로 성능이 저하되는 사례가 확인되어 중요도 상향 |
| 클립형 보조 LED 조명 | 추가 권장 | 역광·조도 변화 통제 |
| HDMI 케이블/어댑터 | 추가 권장 | Dashboard 대형 화면 연결용 |
| Raspberry Pi AI Kit (Hailo-8L, 13 TOPS) | **필수로 격상 권장** | 세그멘테이션은 탐지 대비 연산량이 수 배 크며, CPU 단독 구성은 실시간 데모 품질을 만족시키기 어려운 것으로 확인됨. 예산 여유(34~40만 원 선) 감안 시 처음부터 포함 |
| 3D 프린팅 필라멘트 | 필수 | 하우징 제작 |
| 시연 모형 재료 | 필수 | 도로·Drain 모형(고정 지그 방식 유지) |
| Wi-Fi 공유기(휴대용) | 권장(격상) | 현장 통신 리스크가 가장 흔한 데모 실패 요인이므로 예산 허용 시 필수화 |

예산: AI Kit 포함 시에도 약 50만 원 내에서 여유 확보 가능. GPS 모듈·차량 브래킷은 저가 부품으로 예산에 큰 영향 없음.

## 5. 소프트웨어 파이프라인

```
[영상(사전 촬영 또는 실카메라)] → [YOLO 세그멘테이션 추론] → [상태·차폐율 계산]
→ [GPS 좌표 획득 → 최근접 빗물받이 매칭] → [상태 변화 시 HTTP POST] → [서버]
→ [WebSocket/Polling] → [Dashboard 실시간 갱신]
```

- Pod ↔ 서버: REST(HTTP POST), 재시도 로직 포함.
- 서버 ↔ Dashboard: 개발 여유 있으면 WebSocket, 촉박하면 1초 간격 Polling으로 대체 가능.
- 개발·검증 환경: 안드로이드 기기가 없으므로 노트북(macOS Continuity Camera 또는 iOS 스트리밍 앱) 또는 WSL2(Windows 리눅스 환경)에서 Pod 로직을 사전 검증 후, 카메라 캡처 부분만 `picamera2`로 교체해 라즈베리파이로 이식.
- Dashboard는 신호의 발신원(Pi vs 시뮬레이터)을 구분하지 않으므로, 시뮬레이터 검증 결과를 그대로 최종 데모 백업으로 활용 가능.

## 6. 데이터 파이프라인 (제작 기한 이전 사전 작업)

1. **촬영**: CLEAR/BLOCKED/OCCLUDED 클래스당 80~100장 이상, 거리·각도·조명·이물질 종류를 다양화. 각 Drain(A/B/C) 위치·각도에서 baseline(완전 CLEAR) 사진 별도 확보.
2. **라벨링**: Roboflow에서 Smart Polygon으로 폴리곤 라벨링(클래스 동시 태깅).
3. **파인튜닝**: Google Colab 무료 GPU(T4) 사용, `yolov8n-seg.pt` 기반 전이학습, Roboflow → Colab 연동 export.
4. **검증 (변경)**: mAP 확인, baseline 대비 차폐율 계산 로직 테스트에 더해 — **파인튜닝 완료 즉시 NCNN INT8로 export한 뒤 Pi 5 CPU 단독 실측 FPS를 측정**. 세그멘테이션은 탐지 대비 연산량이 크게 늘어나는 것으로 확인되어, 이 실측 결과가 3FPS 미만이면 AI Kit(Hailo-8L) 투입을 즉시 확정한다(4장 BOM에서 필수로 격상한 이유).
5. **이식**: 완성된 가중치 파일을 제작 당일 라즈베리파이로 이전, 카메라 인터페이스만 교체.

## 7. 데모 구성

- 라이브 시연 리스크(현장 Wi-Fi, 조명)를 줄이기 위해, 사전 촬영한 Pod 실제 카메라 영상에 파인튜닝된 모델이 실시간으로 박스/마스크를 그리는 장면과, 그 판정이 서버로 전송되어 Dashboard 카드가 즉시 반응하는 장면을 화면 분할로 동시 녹화한 백업 시연 영상을 제작.
- 시연 Pod는 3.1의 고정 지그 방식(3개 Drain A/B/C를 정해진 경로로 통과)을 그대로 사용한다. 이는 "차량 탑재형 배치를 축소 모사한 시연 장치"로 명확히 설명하며, 3.2에서 밝힌 실제 차량 환경과의 차이(거리·각도 편차)는 로드맵 한계로 별도 슬라이드에 명시한다.
- Dashboard에는 기존 GREEN/RED/ORANGE 상태 카드 외에 예측 가중치 기반 우선순위 지도를 추가하여, 감지·예측 두 레이어가 결합된 흐름을 시각적으로 증명.
- **예측 레이어 시연 시나리오(v2.3 개정)**: 핵심 장면을 "특정 구역 강수확률 상승 → 지도 부각"에서 "**실시간 차폐율 감지 → 우선순위 즉시 재계산**"으로 재구성했다(2.2절 가중치 개정과 연결). Pod가 특정 drain을 OCCLUDED로 판정해 전송하는 순간, 해당 drain의 priority_score가 대시보드에서 즉시 상승하고 카드가 플래시되는 장면을 핵심으로 시연한다. 여기에 더해, 발표 중 `POST /api/priority/refresh`를 수동 트리거해 내일자 강수확률을 조회하고, 임계값을 넘으면 대시보드 상단에 "내일 천안시 전역 호우 예보" 배너가 뜨는 장면을 보조로 시연한다(2.2-1절) — 두 장면을 합쳐 "지금 막힌 곳은 즉시, 내일 올 비는 전체 순찰팀에게 미리" 두 갈래 대응 서사를 완성한다.
- **판정 지연(latency)에 대한 설명 방침**: 세그멘테이션 추론 특성상 시연 중 프레임 간 지연이 눈에 띌 수 있다. 이는 기술적 결함이 아니라 **"제한된 예산으로 고성능 가속기 대신 저비용 구성(Pi CPU 또는 보급형 AI Kit)을 선택한 결과"**로 정면 설명한다. 발표 스크립트 예시: "본 데모는 학생 팀의 제한된 예산 내에서 상용 등급이 아닌 개념 증명(MVP) 수준의 하드웨어로 구현되었습니다. 실제 지연은 더 높은 사양의 가속기(예: Hailo-8, 클라우드 GPU 추론)를 투입하면 즉시 해소되는 하드웨어 스케일링 문제이며, 알고리즘 자체의 한계가 아닙니다." 이 설명은 5번(AI Kit 미탑재 시) 또는 3.2(baseline 오차)와 함께 "예산 제약 인정 → 로드맵으로 해결"이라는 일관된 톤으로 발표 전반에 배치한다.
- 시뮬레이션 환경(WSL/노트북)에서 구동한 시연은 "동일 API 구조로 구동한 Pod 시뮬레이션"임을 발표에서 명시.

## 8. 확인이 아직 필요한 항목 (부서 협의 트랙 제외, 전량 로드맵으로 유지)

본 프로젝트는 제작 기한(1박 2일)의 제약상 천안시 관련 부서와의 직접 협의를 진행하지 않았으며, 아래 항목은 모두 **공개 자료 기반의 가정이며 실제 도입 시 검증이 필요한 로드맵 항목**으로 발표에 명시한다.

- **우천 시 카메라 성능 저하**: MVP 단계의 명확한 한계로 인정한다(숨기지 않음). 다만 본 시스템의 핵심 유즈케이스는 "폭우 중 실시간 감지"가 아니라 **강수 예보 발생 이전의 사전 예방적 준설**(2.2절 동적 예측 레이어와 직결)이므로, 실제 순찰·준설 활동은 대부분 맑은 날 또는 비가 그친 직후 이루어진다는 점에서 이 한계가 시스템의 존재 이유를 훼손하지는 않는다는 논리로 발표에서 방어한다. 실제 제작될 Pod 버전에는 저조도·적외선 카메라로 보완 예정임을 로드맵에 기재.
- **통수량·정비이력 데이터**: 현재 미확인 상태로 로드맵 항목 유지. 공공데이터포털·UPIS에는 관경/통수량 등 수리학적 데이터가 공개되어 있지 않음을 확인했으며, 실제 도입 시 관련 부서와의 협의가 필요하다는 점만 언급하고 협의 진행 여부는 발표에서 별도로 강조하지 않는다.
- **차량 탑재 시 거리·각도 편차로 인한 차폐율 오차** (3.2 신규 반영): MVP 단계의 명시적 한계이며, 표준 규격 기반 무보정 방식 또는 ToF 센서 보강이 Phase 2 해결 방향.
- **GPS 위치 매칭 정확도** (2.1·2.3 신규 반영): 노선 기반 매칭(고정 노선 차량에 한해 후보군을 사전 등록된 빗물받이 목록으로 제한)으로 저가 GPS의 수 미터 오차로 인한 오매칭 위험을 완화했으나, 노선 미등록 차량·동일 노선 내 근접 grate 구간에서는 잔여 혼동 가능성이 남아 있음 — RTK-GPS 또는 진행 방향·통과 순서 기반 보정이 Phase 2 해결 방향.
- **Pi CPU 단독 추론 시 세그멘테이션 모델의 실제 프레임레이트**: 6장에서 명시한 대로 파인튜닝 직후 조기 실측하여 AI Kit 투입 여부를 결정한다.

## 9. 프로젝트 구성 (시스템 아키텍처 및 기술 스택)

### 9.1 기술 스택 확정

| 레이어 | 선택 | 선정 이유 |
|---|---|---|
| 엣지(Pod) | Python (ultralytics, picamera2) | 5장에서 이미 확정된 파이프라인 |
| **백엔드** | **Python + FastAPI** | 엣지와 언어를 통일해 코드·검증 로직 재사용 가능(예: 차폐율 계산 함수 공유). 비동기 처리·WebSocket이 기본 내장되어 있어 5장에서 언급한 "여유 있으면 WebSocket, 촉박하면 Polling" 전환이 코드 수정 없이 쉬움. ORM은 SQLAlchemy, 스키마 검증은 Pydantic 사용을 권장 |
| **프론트엔드(대시보드)** | **React** | 확정 |
| **DB** | **SQLite (파일 기반)** | 데모 규모(빗물받이 3~수백 개소)에서 PostgreSQL+Docker는 과설계이며, 심사가 발표·스토리텔링 중심임을 감안하면 "왜 이 선택을 했는가"를 설득력 있게 설명할 수 있는 실용적 판단이 더 유리함. SQLAlchemy ORM을 그대로 사용하므로 연결 문자열 한 줄만 바꾸면 향후 PostgreSQL로 전환 가능 — 이 마이그레이션 경로 자체를 "확장성을 설계에 반영했다"는 발표 근거로 제시한다. 별도 컨테이너·서버 프로세스가 불필요해 제작 당일 인프라 셋업 시간도 단축 |

### 9.2 전체 데이터 흐름

```
[차량 탑재 Pod / 시뮬레이터]                    [기상청 단기예보 API]
   ├─ YOLOv8n-seg 추론 (상태 + 차폐율%)              │ (천안시 대표 좌표 1곳만, 내일자 강수확률+강수량)
   ├─ GPS 좌표 획득 → 담당 노선 후보군 내 최근접 매칭    │ (2.2-1·2.2-2절)
   └─ HTTP POST /api/detections  (JSON)              │
            │                                        │
            ▼                                        ▼
   [백엔드 - FastAPI] ◄─────────────────────────────────
   ├─ 요청 검증 (Pydantic)
   ├─ SQLite 저장 (detections 테이블 INSERT + drains 테이블 최신 상태 UPSERT)
   ├─ 날씨 모드(NORMAL/RAIN/HEAVY_RAIN) 판정 → 가중치 프로필 선택 (2.2-2절)
   ├─ 정적 위험도(지대·침수이력) + 실시간 차폐율을 그 가중치로 우선순위 점수 재계산 (2.2절)
   ├─ 폭우 예보(HEAVY_RAIN)면 시스템 전역 경고 배너 갱신 (2.2-1절, drain별 반영 아님)
   └─ WebSocket 브로드캐스트 (또는 프론트가 1초 간격 Polling)
            │
            ▼
   [프론트엔드 - React 대시보드]
   ├─ 초기 로드: GET /api/drains + GET /api/weather/alert
   ├─ 실시간 갱신: WebSocket 수신 또는 Polling
   └─ GREEN/RED/ORANGE 상태 카드 + 우선순위 지도 + 전역 호우 알림 배너 렌더링
```

### 9.3 DB 스키마 (SQLite)

```sql
-- 차량 테이블 (Pod가 탑재된 차량 식별 + 담당 고정 노선, 2.3절)
CREATE TABLE vehicles (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    vehicle_code  VARCHAR(50) NOT NULL,   -- 예: 'GARBAGE-01', 'BUS-100'
    vehicle_type  VARCHAR(20),            -- 'garbage_truck' / 'bus' / 'patrol' 등
    route_id      INTEGER                 -- 담당 고정 노선 (쓰레기 수거 구역, 버스 노선 등)
);

-- 빗물받이 마스터 테이블 (공공데이터포털 좌표 기반 시드 데이터 + 데모용 A/B/C 3개소)
CREATE TABLE drains (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    external_code       VARCHAR(50),      -- 공공데이터포털 도형관리번호 (있을 경우)
    name                VARCHAR(100) NOT NULL,   -- 예: 'Drain A', '성정동 12-3'
    lat                 REAL,
    lng                 REAL,
    elevation           REAL,             -- DEM 대신 Open-Elevation API로 조회한 실측/더미 지대 높이
    is_flood_zone       BOOLEAN DEFAULT 0,   -- 환경부 지정 침수 중점관리구역 여부
    priority_score      REAL DEFAULT 0,   -- 정적 위험도(지대·침수이력) + 미점검 경과 + 실시간 차폐율 결합 산출값 (2.2절, v2.6)
    priority_reasons    TEXT,             -- 우선순위 근거 문구(JSON 배열, 기여도 큰 순)
    elevation_risk      REAL,
    flood_history_flag  REAL,
    staleness_risk      REAL,             -- 마지막 점검 후 경과일이 날씨 모드별 기준에 얼마나 근접했는지, 0~1 (v2.6)
    occlusion_norm      REAL,
    last_status         VARCHAR(20),      -- CLEAR / BLOCKED / OCCLUDED / UNASSESSABLE
    last_occlusion_pct  REAL,
    last_updated        TIMESTAMP,
    maintenance_note        TEXT,       -- 사람이 남긴 조치/점검 완료 메모 (2.2-3절, v2.8)
    maintenance_resolved_at TIMESTAMP   -- last_updated보다 최신이면 "AI 재확인 전인 해결 상태"로 간주
);
-- 강수확률(forecast_rain_prob)은 v2.3부터 drain별 컬럼으로 저장하지 않는다.
-- 천안시 전역이 사실상 동일한 예보 격자권이라 drain마다 반복 저장할 이유가 없어졌고,
-- 대신 시스템 전역 알림 상태(2.2-1절)로 백엔드 인메모리에서만 관리한다 — 별도 테이블 없이
-- 데모 스코프에 맞춰 단순화했으며, 재시작 시 초기화되는 점은 로드맵에서 영속화(테이블화) 대상으로 남긴다.

-- 빗물받이 <-> 노선 다대다 조인 테이블 (v2.4 신규)
-- drains.route_id 단일 컬럼에서 전환 — 같은 grate가 쓰레기차 노선과 버스 노선 양쪽에
-- 동시에 걸칠 수 있다는 2.3절 확장 전략의 현실을 반영. drain 하나가 route 여러 개를 가질 수 있음.
CREATE TABLE drain_routes (
    id       INTEGER PRIMARY KEY AUTOINCREMENT,
    drain_id INTEGER REFERENCES drains(id),
    route_id INTEGER NOT NULL
);

-- 판정 이력 테이블 (Pod가 보낸 원시 이벤트 로그)
CREATE TABLE detections (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    drain_id        INTEGER REFERENCES drains(id),
    vehicle_id      INTEGER REFERENCES vehicles(id),  -- 어느 차량의 Pod가 보낸 판정인지 (2.3절)
    status          VARCHAR(20) NOT NULL,
    reason_code     VARCHAR(40),      -- UNASSESSABLE일 때 DRAIN_NOT_DETECTED
    occlusion_pct   REAL,
    confidence      REAL,
    source          VARCHAR(20),      -- 'pi' / 'simulator'
    captured_at     TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 물리적 판정과 분리된 장치·통신·파이프라인 이벤트
CREATE TABLE system_events (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    drain_id    INTEGER REFERENCES drains(id),
    vehicle_id  INTEGER REFERENCES vehicles(id),
    event_type  VARCHAR(40) NOT NULL, -- RESULT_MISSING 등
    detail      TEXT,
    source      VARCHAR(20),
    occurred_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_detections_drain_id ON detections(drain_id);
CREATE INDEX idx_drain_routes_drain_id ON drain_routes(drain_id);
CREATE INDEX idx_drain_routes_route_id ON drain_routes(route_id);
```

`vehicles`는 차량-노선 등록, `drains`는 물리 상태 스냅샷, `drain_routes`는 다대다 관계, `detections`는 AI 판정 이력, `system_events`는 장치·통신 이벤트로 역할을 분리한다. 특히 `RESULT_MISSING`을 `detections`에 넣지 않아 운영 오류가 빗물받이 상태처럼 해석되는 것을 막는다.

### 9.4 API 명세 (MVP 최소 구성)

| Method | Endpoint | 설명 | 호출 주체 |
|---|---|---|---|
| POST | `/api/detections` | 방문 단위 판정 결과 전송. 대상 인식 실패 시 `status=UNASSESSABLE`, `reason_code=DRAIN_NOT_DETECTED`, `occlusion_pct=null` | Pod → 백엔드 |
| GET | `/api/drains` | 전체 빗물받이 최신 상태 + 우선순위 점수 목록 | 프론트 초기 로드 |
| GET | `/api/drains/{id}/history` | 특정 빗물받이 판정 이력(페이지네이션 지원) | 프론트 상세 보기 |
| GET | `/api/system-events` | `drain_id`로 필터 가능한 장치·통신 이벤트 조회. 판정 이력과 별도 표시 | 프론트 상세 보기 |
| WS | `/ws/dashboard` | 신규 판정·전역 알림 발생 시 실시간 푸시. 끊기면 프론트가 1초 Polling으로 자동 대체 후 지수 백오프로 재연결 | 프론트 |
| POST | `/api/priority/refresh` | 날씨 모드(2.2-2절)를 먼저 갱신한 뒤, 그 모드의 가중치 프로필로 전체 drains의 priority_score를 재계산(2.2절). 폭우 모드면 시스템 전역 경고도 함께 갱신(2.2-1절). 백엔드 내부 스케줄러(주기 실행)로도 동작하지만, 발표 중 즉시 시연 가능하도록 수동 트리거용으로도 겸용 제공 | 백엔드 스케줄러 / 발표자 수동 트리거 |
| GET | `/api/weather/alert` | 현재 날씨 모드·점검 임계값·호우 경고 상태 조회(2.2-1·2.2-2절) — 대시보드 초기 로드 및 상시 모드 표시용 | 프론트 초기 로드 |
| POST | `/api/weather/mode` | 날씨 모드를 NORMAL/RAIN/HEAVY_RAIN 중 하나로 강제 전환하거나(발표용 수동 스위치, 2.2-2절 신규, v2.9), mode="AUTO"로 실제 예보 기반 판정에 즉시 재동기화. 전환 즉시 전체 drains의 priority_score를 재계산해 브로드캐스트하며, 수동 전환 중에는 `/api/priority/refresh`가 이 상태를 덮어쓰지 않음 | 프론트 모드 전환 버튼 / 발표자 수동 시연 |
| POST | `/api/telemetry` | 매 주기 보내는 위치 신호. drain 근접 후 일정 시간 판정이 오지 않으면 `system_events`에 `RESULT_MISSING`을 기록하며 drain 상태·판정 이력은 변경하지 않음 | Pod → 백엔드 |
| POST | `/api/route/plan` | 팀 수(team_count) 입력 시, 현재 날씨 모드의 공통 점검 게이트(2.2-2절)를 넘는 빗물받이를 위치 기준으로 군집화한다. 각 군집의 최고 우선순위 지점을 첫 방문지로 고정하고, 나머지는 최근접 이웃+2-opt로 왕복을 줄인다. 팀 내 거리에는 OSRM 실도로망을 쓰며 실패 시 직선거리로 자동 대체하고 `distance_source`로 밝힌다. `min_occlusion_pct`/`min_priority_score`를 지정하면 기본 게이트 대신 해당 기준을 쓴다. | 프론트 동선 추천 패널 |
| POST | `/api/drains/{id}/resolve` | 사람이 조치(BLOCKED/OCCLUDED 청소) 또는 점검(미점검 상태 육안 확인)을 완료했음을 기록(2.2-3절 신규, v2.8). AI의 last_status/차폐율은 위조하지 않고, 처리 시각만 별도로 남겨 미점검 경과일 계산의 새 기준점으로 삼는다 | 프론트 "조치 완료"/"점검 완료" 버튼 |
| GET/POST | `/api/vehicles` | 차량 목록 조회 / 신규 차량 등록(2.3절 "차량 1대 추가 = 행 하나" 확장 스토리를 실제로 시연 가능, v2.4 신규) | 프론트 / 발표자 수동 등록 |
| GET | `/api/vehicles/{id}/drains` | 특정 차량의 실제 판정만 조회하며 시스템 이벤트는 섞지 않음. 차량 선택 시 해당 지점을 지도에 표시 | 프론트 차량별 조회 패널·지도 |
| GET | `/api/elevation` | 위경도로 실제 지대 고도 조회(Open-Elevation API 연동, v2.4 신규) — drains_seed.json에 실측값을 채워넣는 용도 | 발표자 수동 조회 |
| GET | `/api/flood-zone`(`/raw`) | 위경도로 행안부 생활안전지도 침수흔적 조회(SAFEMAP_API_KEY 필요, 응답 필드명 미확정이라 `/raw`로 원본 노출) — drains_seed.json의 is_flood_zone을 채워넣는 용도 | 발표자 수동 조회 |

인증/인가는 MVP 범위에서 제외한다(로드맵 항목: 실 서비스 전환 시 API 키 또는 JWT 인증 추가 필요). 데모 환경은 폐쇄망(현장 Wi-Fi 공유기)에서만 구동되므로 리스크가 제한적이다.

### 9.5 저장소 구조 (Docker 불필요 — SQLite 파일 기반)

```
drain-vision-pod/
├── edge/                  # 라즈베리파이(Pod)에서 구동되는 코드
│   ├── infer.py           # YOLOv8n-seg 추론 + 차폐율 계산
│   ├── gps_match.py        # GPS 좌표 → 담당 노선 후보군 내 최근접 drain 매칭, 반경 초과 시 매칭 실패 처리 (2.1절)
│   ├── sender.py           # 백엔드로 HTTP POST 전송(판정) + 매 주기 위치 텔레메트리 전송
│   ├── run.py               # capture→infer→match→집계(방문 단위 이상값 제거)→전송 메인 루프
│   └── baseline_pixels.json  # drain별 baseline 픽셀 수 (3.1절)
├── backend/                # FastAPI 서버
│   ├── main.py              # 엔드포인트 전체 + 판정 미수신 RESULT_MISSING 시스템 이벤트
│   ├── models.py            # SQLAlchemy 모델 (vehicles, drains, drain_routes, detections, system_events)
│   ├── schemas.py           # Pydantic 스키마
│   ├── forecast.py          # 기상청 단기예보 API 조회 + 날씨 3단계 모드 판정 (2.2-1·2.2-2절)
│   ├── routing.py           # 위치 군집 + 순수 거리 기준 최근접 이웃/2-opt·or-opt(OSRM, 실패 시 haversine)
│   ├── elevation.py         # Open-Elevation API 연동 (v2.4 신규)
│   ├── flood.py             # 행안부 생활안전지도 침수흔적 조회 (응답 필드명 미확정, 신규)
│   ├── geo.py               # haversine 거리 계산 공용 함수
│   ├── .env / .env.example   # KMA_API_KEY 등 비밀값 분리 (v2.4 신규)
│   ├── drainvision.db      # SQLite 파일 (init.sql로 최초 1회 초기화, git에는 미포함)
│   └── requirements.txt
├── frontend/                # React 대시보드
│   ├── src/
│   └── package.json
└── db/
    ├── init.sql             # 9.3 스키마 초기화 스크립트 (테이블 정의만)
    └── drains_seed.json      # 시드 데이터(vehicles/drains, route_ids 배열) — 좌표를 바꾸는 유일한 출처
```

**PostgreSQL+Docker에서 SQLite로 전환한 이유**: 데모 규모(빗물받이 3~수백 개소)에서 별도 DB 서버·컨테이너를 띄우는 것은 제작 시간 대비 이득이 낮은 과설계다. SQLite는 파일 하나로 즉시 구동되어 컨테이너 기동 대기 시간이 사라지고, 팀원 전원이 동일 환경을 맞추는 부담도 줄어든다. 심사가 발표·스토리텔링 중심임을 감안하면 "왜 이 선택을 했는가"를 설득력 있게 설명할 수 있는 실용적 판단이 화려한 스택보다 유리하다. 확장 스토리는 SQLAlchemy ORM 덕분에 연결 문자열만 바꾸면 PostgreSQL로 전환 가능하다는 점으로 방어한다(발표 문구 예시: "데모는 SQLite로 가볍게 구동했지만, 실 서비스 규모(6만여 개소)로 전환 시 ORM 계층은 그대로 두고 DB만 PostgreSQL로 교체하면 됩니다").

### 9.6 개발 순서 제안 (팀 역할 분배 기준)

1. `db/init.sql` 스키마 확정 및 SQLite 파일 초기화(데모용 vehicles·route 시드 데이터 포함) (병행 가능, Docker 기동 대기 없이 수 분 내 완료).
2. 백엔드: `POST /api/detections`, `GET /api/drains` 두 엔드포인트부터 구현 — 이 둘만 있어도 6장 파이프라인 검증(시뮬레이터 → 서버 → 더미 응답)이 가능.
3. 백엔드: `forecast.py`에서 priority_score 재계산 로직(2.2절, 날씨 모드별 가중치 프로필) 및 천안시 대표 좌표 1곳에 대한 기상청 단기예보 API 연동(2.2-1·2.2-2절, 날씨 3단계 모드 판정+시스템 전역 알림) 구현 — API 키는 공공데이터포털에서 즉시 발급 가능하므로 1번과 병행 착수 가능.
4. 프론트: `GET /api/drains`를 1초 Polling으로 호출하는 대시보드 카드 UI부터 구현 — WebSocket은 시간이 남을 경우의 스트레치 목표로 유지. 차폐율 변화에 우선순위 지도가 즉시 반응하는 장면과, 전역 호우 알림 배너(2.2-1절)를 7장 데모 시나리오와 연결.
5. 엣지 Pod 코드(`infer.py`)와 백엔드 연동은 가장 마지막 — 6장에서 이미 계획된 대로 노트북/WSL 시뮬레이터로 먼저 검증 후 라즈베리파이로 이식.
