# Codex repository instructions

Before starting any repository task, read and follow `CLAUDE.md` in the repository root. It is the canonical source for the project's architecture, commands, constraints, and document conventions.

Treat references to Claude Code inside shared guidance as applying to the active coding agent unless a rule explicitly concerns a Claude-only feature such as `.claude/settings.local.json`, Claude hooks, or Claude permissions.

If this file and `CLAUDE.md` ever conflict, follow this file for Codex-specific behavior and otherwise follow `CLAUDE.md`.
